# provider continuity needs deployment convergence

problem: the former skid source selected fresh provider homes, hiding existing
authentication, configuration, history and memories without an identity need.
switching paths alone was unsafe because dev-server replaced codex `hooks.json`.

source correction: profiles and marked shells select existing accounts; personal
claude leaves `CLAUDE_CONFIG_DIR` unset. the unused codex hook writer/template is
removed. the explicit claude plugin requires a skid provider launch marker and
rejects herdr context before reading input or config. disposable account, shell,
hook admission and codex projection probes passed; no live provider was used.

blocker: dev-server's owner acknowledged the hook contract but still awaits its
owner's answer on the shared-account change. its installer must stop provisioning
provider homes and replacing hooks, settings or instruction files. the prior
receipt acknowledgment does not qualify this change.

resolved when: reviewed dev-server source renders these profiles, preserves
existing account files and native herdr/user integrations, removes the codex
hook installation dependency, and its owner acknowledges the revised contract.
live behavior remains separately tracked in `restoration-runtime.md`. shared
account changes made in either app are intentionally visible in the other.
